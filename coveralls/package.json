{
  "name": "coverage",
  "dependencies": {
    "nyc": "^15.1.0",
    "nyc-report-lcov-absolute": "^1.0.0"
  },
  "scripts": {
    "merge_coverage": "nyc --temp-dir=../js_coverage/ --reporter=nyc-report-lcov-absolute report"
  }
}
