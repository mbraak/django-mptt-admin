{
  "name": "coverage",
  "dependencies": {
    "nyc": "^15.1.0"
  },
  "scripts": {
    "merge_coverage_lcov": "nyc --temp-dir=../js_coverage/ --reporter=lcovonly report",
    "merge_coverage_json": "nyc --temp-dir=../js_coverage/ --reporter=json report"
  }
}
